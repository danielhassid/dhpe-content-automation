# CLAUDE.md — כללי-על לריפו dhpe-content-automation

הריפו מכיל שתי מערכות סוכנים של דניאל: **מערכת התוכן** (שחר הכותב, סיסי המבקרת,
יועץ SEO) ו**שולחן המסחר ביננס** (אלון, תמר, יואב, נועה, מרים, איתן, גדי).

## כללים משותפים לשתי המערכות

1. **סודות לעולם לא בריפו.** אין סיסמאות, אין מפתחות API, אין טוקנים — לא בקבצים,
   לא ב-RUN-LOG, לא בפלט debug, לא בהודעות commit. (תקרית עבר: סיסמת WordPress
   הודבקה ל-RUN-LOG ונכנסה ל-git. לא חוזר.) סודות = env vars / secrets של הסביבה בלבד.
2. **תקשורת בין סוכנים דרך קבצים בלבד.** כל סוכן כותב את קובץ החוזה שלו ותו לא.
3. **מקור אמת יחיד.** מחירים — `pricing.md`; מגבלות מסחר — `trading-desk/config/limits.json`.
   לא מצטטים מהזיכרון מספר שיש לו קובץ.
4. **שפה:** עברית נקייה, מונחי אנגלית בסוגריים בסוף המשפט. טון ישיר, בלי ריכוך.
5. **אין המצאת נתונים.** אין נתון = אומרים "אין נתון". הערכה מסומנת כהערכה.

## כללי שולחן המסחר (מחייבים כל סוכן וכל סשן)

- **איתן (binance-trade-executor) הוא היחיד שמבצע**, ורק דרך
  `scripts/binance/place_order.py`, ורק הצעות APPROVED טריות מפסק הדין של מרים.
- **מרים (binance-risk-manager) היא היחידה שמאשרת.** הווטו שלה סופי להיום.
- **קבצים שרק דניאל עורך ידנית:** `trading-desk/config/limits.json`,
  `trading-desk/config/phase.json`. סוכן שרוצה שינוי — מציע בכתב, לא עורך.
- **KILL-SWITCH:** קיום הקובץ `trading-desk/KILL-SWITCH` עוצר כל ביצוע מיידית.
  דניאל ומרים רשאים ליצור; רק דניאל מוחק.
- **מודל שלבים:** Phase 0 נייר (המצב הנוכחי) → Phase 1 testnet → Phase 2 חי.
  קידום שלב = עריכה ידנית של דניאל ב-phase.json, אחרי הצ'קליסט ב-PROGRESS.md.
- **מפתחות ביננס:** env vars בלבד (`BINANCE_API_KEY/SECRET`,
  `BINANCE_TESTNET_API_KEY/SECRET`). בצד ביננס: spot trade בלבד,
  **משיכות (withdrawals) כבויות לתמיד**.
- **עקיבות:** כל פקודה נושאת proposal_id או trigger_id. אין פקודות יתומות.
- **הרצה בסאב-איג'נטים:** בצנרת מלאה כל סוכן רץ כסאב-איג'נט נפרד (Agent tool)
  עם ה-SKILL.md שלו; הסשן הראשי מתאם, מפקח ומבצע רק git. סוכן לא נוגע בקובץ
  שאינו בחוזה שלו.
- לפני כל push: `git grep -inE "(api[_-]?key|secret|password)\s*[:=]" -- ':!CLAUDE.md' ':!*.md.tmpl'`
  ובדיקה ידנית של כל ממצא.

## הערת סביבה

ה-sandbox של claude.ai חוסם egress לרוב היעדים (נבדק 2026-08-16: api.binance.com,
testnet.binance.vision ו-data-api.binance.vision — כולם חסומים, 403 מה-proxy).
לכן: שולחן המסחר רץ כאן במצב נייר/המלצות; `market_watcher.py` חייב מארח חיצוני
קבוע; נתוני שוק בסביבה זו — דרך חיפוש רשת עם ציון מקור.
