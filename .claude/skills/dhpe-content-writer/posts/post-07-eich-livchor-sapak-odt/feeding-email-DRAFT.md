# Feeding email — drafted but NOT sent (SMTP egress still blocked)

**Run date:** 2026-08-11 (יום שלישי — סלוט feeding שלישי 17:00)
**Mode:** feeding-email
**Target post:** איך לבחור ספק ODT מתאים לבית ספר (חודש 1 / שבוע 4 / ראשון, נישה 1 — ODT לבית ספר)
**Focus keyword:** איך לבחור ספק ODT / איך בוחרים ספק ODT לבית ספר
**Angle:** רוב המדריכים ברשת ל"איך לבחור ספק ODT" עוצרים בפרמטרים גנריים — ניסיון, המלצות, מחיר, ביטוח. הפער שהפוסט אמור למלא: מה מנהלים בפועל שוקלים בשיחת המכירה עצמה — קליק, שיקוף, האם הספק כבר "רואה" את הכיתה שלהם. השאלה מבקשת רגע ספציפי משיחת מכירה שבה מנהל בחר בדניאל אחרי התלבטות — לא המחיר, לא הניסיון, אלא מה שקרה בשיחה עצמה. זה מייצר פסקה אמיתית בפוסט שמנהל קורא ומזהה את עצמו בה.
**Status:** ❌ נחסם שוב — SMTP egress (smtp.gmail.com:465 וגם :587) חסום ברמת policy של ה-environment. תקלה חוזרת מזה כמעט שלושה חודשים (מ-2026-05-30) בכל ריצות ה-feeding.

## תוכן המייל שהוכן

**אל:** dh052597@gmail.com
**נושא:** שאלה קצרה לפוסט הבא — איך לבחור ספק ODT מתאים לבית ספר

**גוף:**

```
היי דניאל,

בפוסט הבא עולה: איך לבחור ספק ODT מתאים לבית ספר

הזווית: רוב המדריכים ברשת מדברים בקווים כלליים — "בדקו ניסיון", "קראו המלצות", "השוו מחירים". בלי בשר. מנהלים בפועל שוקלים דברים אחרים לגמרי — קליק בשיחה הראשונה, איך הספק מגיב כשהם מספרים על שכבה בעייתית, האם הוא מציע פתרון גנרי שנשמע כמו מה שכולם מציעים, או שהוא באמת מקשיב ומחזיר משהו שמראה שהוא הבין את הכיתה הזאת ולא כיתה תיאורטית.

יש לי שאלה אחת קצרה — 2-3 משפטים יעשו את ההבדל בין פוסט כללי שאף מנהל לא מזדהה איתו לבין פוסט שמנהל קורא ואומר "כן, זה בדיוק מה שקרה אצלי":

---

תזכור לי מקרה קונקרטי אחד שבו מנהל או מנהלת התלבטו בינך לבין עוד ספק — ובחרו בך. מה בדיוק בשיחה איתך גרם להם להחליט. לא "כי יש לך ניסיון" ולא "כי המחיר היה טוב" — משהו ספציפי שקרה בשיחה עצמה: שאלה ששאלת אותם על הכיתה או על השכבה, משהו ששיקפת להם שאף אחד אחר לא שיקף, רגע שהם הבינו שאתה כבר רואה את הבעיה שלהם ולא רק מוכר להם מוצר. איך הם עצמם ניסחו את זה כשחזרו אליך עם ה"כן"?

---

אין חובה לענות. אם לא תגיב עד 07:30 ביום הפרסום, הפוסט ייכתב עם האיכות הרגילה — פשוט בלי הסיפור האישי שלך.

DHPE Content System
```

## בדיקות שבוצעו בריצה הזו

```
Attempt 1 — smtplib.SMTP_SSL('smtp.gmail.com', 465, timeout=30) ללא IPv4 patch:
  OSError: [Errno 97] Address family not supported by protocol
  (IPv6 לא נתמך ב-environment; getaddrinfo מחזיר קודם רשומת AAAA)

Attempt 2 — אותה קריאה עם IPv4-only getaddrinfo patch:
  TimeoutError: timed out
  (חיבור TCP ישיר ל-smtp.gmail.com:465 נחסם ב-egress policy)

Probe נוסף:
  timeout 6 bash -c 'cat < /dev/tcp/smtp.gmail.com/465' → exit 124 (timeout)
  timeout 6 bash -c 'cat < /dev/tcp/smtp.gmail.com/587' → exit 124 (timeout)

Proxy check:
  HTTPS_PROXY קיים אבל זה HTTP proxy — לא מנתב SMTP, אין CONNECT ל-:465/:587 במדיניות.
```

## פתרון (חוזר על עצמו כבר חודשיים וחצי)

הוסף את הכניסות הבאות ל-egress allowlist של ה-environment דרך Settings → Network policy ב-Claude Code on the web (https://code.claude.com/docs/en/claude-code-on-the-web):

- `smtp.gmail.com:465`
- `smtp.gmail.com:587`

עד שזה ייפתר — כל ריצת feeding-email תיחסם, וההזנה של דניאל לא תגיע לתיבה לפני הפוסט. המשמעות: הפוסטים ייכתבו בלי הסיפור האישי שלו, וזה בדיוק הדבר שאמור להבדיל את הבלוג של DHPE מהמתחרים.

## למה נבחר הפוסט הזה

- היום 2026-08-11 שלישי, סלוט feeding שלישי 17:00.
- הפוסטים ממשיכים כרונולוגית בתוכנית (post 06 היה feeding-DRAFT האחרון ב-2026-07-18) כי כולם תקועים ב-DRAFT — SMTP חסום, ולכן פוסט 07 (ראשון, שבוע 4, נישה 1 — איך לבחור ספק ODT) הוא הבא ברצף.
- הנושא: מדריך לבחירת ספק ODT. פוסט בליבת "ODT לבית ספר" עם intent גבוה של מנהלים בשלב בחירת ספק — בדיוק ה-bottom-of-funnel שהאתר צריך.

## הערה על השאלה

השאלה בונה על היתרון התחרותי של דניאל: המתחרים מדברים בשפת גיבוש עובדים ארגוני, דניאל מדבר בשפת הרכז והמנהל החינוכי. השאלה מבקשת מקרה קונקרטי משיחת מכירה — לא הצהרה כללית — ומדגישה שהתשובה צריכה להיות מה שקרה בתוך השיחה עצמה, לא רשימת יתרונות. זה מונע סיפור גנרי ומאפשר לכתוב פסקת מקרה בפוסט שנשמעת אמיתית ולא מלאכותית.
